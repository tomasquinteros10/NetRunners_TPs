# Trabajos practicos

Cada TP contiene su propia copia de `logo.png`, creada por la skill `crear-tp`.
Desde un TP ubicado en `tps/<tipo>/tp<numero>/`, la ruta es:

```text
logo.png
```

No uses rutas hacia recursos compartidos fuera de la carpeta del TP.
